import "❌ TypeScript → No  ✅ ESLint → Yes  ✅ Tailwind → Yes  ❌ Use `src/` folder → No  ✅ App Router → Yescd mhc-automationcode .@/styles/globals.css";
import type { AppProps } from "next/app";

export default function App({ Component, pageProps }: AppProps) {
  return <Component {...pageProps} />;
}
